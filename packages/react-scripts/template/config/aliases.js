/* 
    Define custom aliases:
        - aliases are defined under src/ scope!
        - aliases are written capitalized
        - aliases are injected into webpack, jest and jsconfig for vscode
        - you can override existing aliases
*/

module.exports = {
  /* 
    e.g. for: import Foo from 'src/components/Foo'
    you'd defined following alias and use it as: import Foo from 'Components/Foo'
  */
  // Components: 'components',
};
